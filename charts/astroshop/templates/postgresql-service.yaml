apiVersion: v1
kind: Service
metadata:
  name: postgresql
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local
  {{- $otel := index .Values "opentelemetry-demo" }}
  {{- with $otel.components.postgresql.service.loadBalancerSourceRanges }}
  loadBalancerSourceRanges:
  {{- toYaml . | nindent 4 }}
  {{- end }}
